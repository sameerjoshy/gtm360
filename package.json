{
  "name": "gtm360",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix apps/web",
    "build:web": "npm run build --prefix apps/web",
    "dev:api": "cd services/api && uvicorn app.main:app --reload"
  }
}